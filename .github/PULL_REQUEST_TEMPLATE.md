Fixes #

**What changes:** <one to three plain sentences of what a user can do afterwards, or what stops going wrong>

**Schema gate:** nothing changed / additive with a default / migration `<revision>` — tested on a populated database and idempotent on a second run

**User-guide gate:** `docs/user/README.md` — <bullet added or changed> / no user-facing change
